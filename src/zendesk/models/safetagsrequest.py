"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict
from zendesk.types import BaseModel, UNSET_SENTINEL


SafeUpdate = Literal[
    "true",
    "false",
]
r"""Enable safe update mode to prevent collisions"""


class SafeTagsRequestTypedDict(TypedDict):
    tags: List[str]
    r"""An array of tag strings to add or set"""
    updated_stamp: NotRequired[datetime]
    r"""The ticket's latest updated_at timestamp for safe updates.
    Must match the current timestamp to prevent collision.

    """
    safe_update: NotRequired[SafeUpdate]
    r"""Enable safe update mode to prevent collisions"""


class SafeTagsRequest(BaseModel):
    tags: List[str]
    r"""An array of tag strings to add or set"""

    updated_stamp: Optional[datetime] = None
    r"""The ticket's latest updated_at timestamp for safe updates.
    Must match the current timestamp to prevent collision.

    """

    safe_update: Optional[SafeUpdate] = None
    r"""Enable safe update mode to prevent collisions"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["updated_stamp", "safe_update"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
