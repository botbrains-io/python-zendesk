# SortBy

One of `updated_at`, `created_at`, `priority`, `status`, or `ticket_type`. Defaults to sorting by relevance

## Example Usage

```python
from zendesk.models import SortBy
value: SortBy = "updated_at"
```


## Values

- `"updated_at"`
- `"created_at"`
- `"priority"`
- `"status"`
- `"ticket_type"`
